# Application
PORT=3000

# MongoDB Atlas
# Never commit real credentials. Use a local .env file or your deployment secret manager.
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/pkmp?retryWrites=true&w=majority&appName=Cluster0
